
# two types
# implicit, the python interpreter do itself based on the priority

# explicit, we do manually

a = 2   # type int
b = 2.3 #type float

sum = a + b     # a is converted implicitly to float (float has high priority means stores more value and space than int )
